/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets.aceternity.com', 'avatar.vercel.sh', 'images.pexels.com' ],
      },
};

export default nextConfig;
