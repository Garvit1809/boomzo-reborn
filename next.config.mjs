/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets.aceternity.com', 'avatar.vercel.sh', 'images.pexels.com', 'unito.io' ],
      },
};

export default nextConfig;
